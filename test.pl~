male(albert).
male(bob).

female(alice).

parent(albert, alice).
parent(alice, bob).



get_grandparent(Y,X) :-
    parent(Z,X),
    parent(Y,Z).

related(X,Y) :-
    parent(X,Y).

related(X,Y):-
    parent(X,Z),
    related(Z,Y).
